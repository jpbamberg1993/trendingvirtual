.well
  %h4 Leave a Comment:
  = form_for([@article, @article.comments.build]) do |f|
    .form-group
      %p
        = f.hidden_field :commenter, value: current_user.name
      %p
        = f.text_area :body, class: 'redactor form-control', id: 'comment-form'
    %button.btn{type: 'submit'}
      = f.submit
